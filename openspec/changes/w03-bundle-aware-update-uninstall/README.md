# w03-bundle-aware-update-uninstall

update/uninstall consume bundle metadata from w02; implements group sync semantics (auto-install new members, warn on removed members with --sync for hard removal); supports local batch directory input for update/uninstall.
